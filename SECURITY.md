# 安全策略

[English](SECURITY_en.md) | [中文](SECURITY.md)

---

## 支持的版本

目前仅维护最新版本（`main` 分支）的安全更新。

| 版本 | 支持状态 |
|------|----------|
| `main` (最新) | ✅ 支持 |
| 旧版本 | ❌ 不再支持 |

---

## 报告安全漏洞

**请勿在公开的 Issue、Pull Request 或讨论区披露安全漏洞。**

如果您发现本项目存在安全漏洞，请通过 **GitHub Security Advisories** 报告：

1. 访问仓库的 **Security** 标签页
2. 点击 **Report a vulnerability**
3. 填写漏洞详情并提交

我们会在收到报告后尽快评估并修复。

---

## 漏洞公开而未修复带来的风险

若安全漏洞在未修复的情况下被公开披露，可能导致以下风险：

| 风险类型 | 说明 |
|----------|------|
| **凭证泄露** | 攻击者可能获取用户配置的 XMRig Proxy `access-token`，进而控制矿池连接、窃取算力或篡改配置 |
| **中间人攻击** | 若漏洞涉及通信链路，攻击者可拦截、篡改浏览器与 Proxy 之间的请求/响应 |
| **XSS / 代码执行** | 前端注入漏洞可能导致恶意脚本在用户浏览器执行，窃取 `localStorage`/`sessionStorage` 中的 Token |
| **拒绝服务** | 精心构造的请求可能导致 Proxy 服务崩溃或面板无法加载，影响监控可用性 |
| **信息泄露** | 敏感运行指标（算力、矿工 IP、上游矿池地址等）可能被未授权访问者获取 |

> **核心原则**：本项目采用**零知识架构**——服务器仅分发静态文件，不知晓用户的 Proxy 地址与 Token。所有 API 请求由浏览器直连用户自建的 Proxy。因此，**漏洞影响范围仅限于使用该面板的用户浏览器与其自建 Proxy 之间**，不波及部署站点（GitHub Pages / Cloudflare Pages 等）及其他用户。

---

## 安全最佳实践（给用户）

为将漏洞影响降至最低，建议用户遵循：

1. **为 Proxy 设置强随机 `access-token`**，定期轮换
2. **在 Proxy 配置中启用 `restricted: true`**，仅开放只读端点（`/1/summary`）
3. **通过 HTTPS 访问面板与 Proxy**（自签证书需导入浏览器信任，或通过 VPN/隧道）
4. **定期清理浏览器存储**，或使用「会话模式」（不勾选「记住我」）
5. **关注本仓库的 Security Advisories**，及时更新面板版本

---

## 致谢

感谢所有负责任地披露漏洞的安全研究人员。

---

## 许可证

Apache License 2.0 © 2025